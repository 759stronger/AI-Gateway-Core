/**
 * @file types.cpp
 * @brief 全局基础类型相关实现预留源文件。
 *
 * 后续实现方向：
 * - 可在此添加 Capability、Role 等枚举到字符串的转换函数。
 * - 可在此添加 Message、Session、GatewayContext 的校验或辅助构造函数。
 * - 保持基础业务类型和其他模块之间的字段含义一致。
 *
 * 当前文件只建立编译单元和模块边界，不提供默认实现，避免改变现有头文件接口。
 */
#include "ai_gateway_core/core/types.h"

namespace ai_gateway_core {

}
