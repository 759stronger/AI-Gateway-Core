/**
 * @file model_mapping.cpp
 * @brief 模型映射数据结构相关实现预留源文件。
 *
 * 后续实现方向：
 * - 可在此添加模型映射校验、默认值构造辅助函数或映射匹配工具函数。
 * - 校验 public_model_name、upstream_model_name、upstream_provider_name、capability 和 price_multiplier。
 * - 与 Storage 和 AdminAPI 保持同一套映射规则。
 *
 * 当前文件只建立编译单元和模块边界，不提供默认实现，避免引入尚未确定的校验规则。
 */
#include "ai_gateway_core/routing/model_mapping.h"

namespace ai_gateway_core {

}
