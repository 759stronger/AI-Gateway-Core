/**
 * @file upstream_account.cpp
 * @brief 上游账号数据结构相关实现预留源文件。
 *
 * 后续实现方向：
 * - 可在此添加上游账号状态转换、字段校验或脱敏展示工具函数。
 * - 校验 account_id、provider_name、base_url、supported_models、capabilities 和配额限制。
 * - 保护 encrypted_api_key 等敏感变量，避免输出明文密钥。
 *
 * 当前文件只建立编译单元和模块边界，不提供默认实现，避免引入尚未确定的安全策略。
 */
#include "ai_gateway_core/upstream/upstream_account.h"

namespace ai_gateway_core {

}
